name: Build auth server
on: { push: { paths: ["app/auth-server/**"] } }
jobs:
  Build-node-js-runner:
    runs-on: self-hosted
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3

      - name: Build docker image
        run: docker build -f ./app/auth-server/auth-server.Dockerfile -t ghcr.io/mp281x/auth-server:1.0.12 ./app/auth-server
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: mp281x
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker push
        run: docker push ghcr.io/mp281x/auth-server:1.0.12
