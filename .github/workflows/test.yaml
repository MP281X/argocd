name: test
on:
  push:
    branches: [main]
  jobs:
    build:
      name: build
      runs-on: self-hosted
      steps:
        - name: Checkout
          uses: actions/checkout@v2

        - name: ls
          run: ls

        - name: echo
          run: echo "ciao"
