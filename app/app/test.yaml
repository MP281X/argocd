apiVersion: apps/v1
kind: Deployment
metadata:
  name: test
  namespace: app-ns
  labels: { app: test }
  annotations: { keel.sh/policy: force }
spec:
  replicas: 1
  selector: { matchLabels: { app: test } }
  template:
    metadata: { labels: { app: test } }
    spec:
      containers:
        - name: test
          image: ghcr.io/mp281x/test-container:latest
